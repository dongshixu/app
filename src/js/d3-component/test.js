/*
 * @Descripttion: 
 * @version: 
 * @Author: xds
 * @Date: 2020-12-16 22:10:59
 * @LastEditors: xds
 * @LastEditTime: 2020-12-18 22:50:33
 */
import * as d3 from 'd3'

  // const xAxis = function() {
  //   const axis = d3.axisBottom()
  //                  .ticks(6)
  //                  .tickSize(size * columns.length);
  //   return g => g.selectAll("g").data(x).join("g")
  //                .attr("transform", (d, i) => `translate(${i * size},0)`)
  //                .each(function(d) { return d3.select(this).call(axis.scale(d)); })
  //                .call(g => g.select(".domain").remove())
  //                .call(g => g.selectAll(".tick line").attr("stroke", "#ddd"));
  // }

  // const yAxis = function() {
  //   const axis = d3.axisLeft()
  //                  .ticks(6)
  //                  .tickSize(-size * columns.length);
  //   return g => g.selectAll("g").data(y).join("g")
  //                .attr("transform", (d, i) => `translate(0,${i * size})`)
  //                .each(function(d) { return d3.select(this).call(axis.scale(d)); })
  //                .call(g => g.select(".domain").remove())
  //                .call(g => g.selectAll(".tick line").attr("stroke", "#ddd"));
  // }


export default {  }